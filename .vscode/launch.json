{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run WowWed",
      "type": "msedge",
      "request": "launch",
      "url": "http://localhost:3001",
      "webRoot": "${workspaceFolder}/frontend/src",
      "preLaunchTask": "WowWed: Run"
    },
    {
      "name": "Run WowWed (Chrome)",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:3001",
      "webRoot": "${workspaceFolder}/frontend/src",
      "preLaunchTask": "WowWed: Run"
    }
  ]
}
